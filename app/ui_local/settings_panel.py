from __future__ import annotations


def build_settings_panel() -> dict[str, str]:
    return {
        "title": "Settings",
        "status": "available",
        "description": "Tunables, model config, and assistant behavior.",
    }